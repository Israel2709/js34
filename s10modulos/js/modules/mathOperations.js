const addTwoNumbers = (firstNumber, secondNumber) => firstNumber + secondNumber;
const substractTwoNumbers = (firstNumber, secondNumber) =>
  firstNumber - secondNumber;
const multiplyTwoNumbers = (firstNumber, secondNumber) =>
  firstNumber * secondNumber;
const divideTwoNumbers = (firstNumber, secondNumber) =>
  firstNumber / secondNumber;

export {
  addTwoNumbers,
  substractTwoNumbers,
  multiplyTwoNumbers,
  divideTwoNumbers,
};

/*
    {
        addTwoNumbers: (firstNumber, secondNumber) => firstNumber + secondNumber,
        substractTwoNumbers:(firstNumber, secondNumber) =>
  firstNumber - secondNumber,
    }
*/

/*
    let a = "foo"
    let b = "bar"
    let someObject = {a, b}

    {
        a:"foo",
        b:"bar"
    }

 */
